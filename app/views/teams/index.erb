<div class="d-flex flex-row justify-content-between align-items-start">
  <h1>Teams</h1>
  <% if current_user&.admin? %>
    <%= link_to 'Edit Results', results_path, class: 'btn btn-danger' %>
  <% end %>
</div>

<%= render "team_list" %>